local HttpError = require(script.Parent.HttpError)

return function()
	describe("new()", function()
		it("should return an object", function()

		end)

		it("should return a formatted table if you call tostring()", function()

		end)
	end)
end